import AppLayout from '../../components/AppLayout';
import Identifikasi from '../../components/Identifikasi';

const IdentifikasiMahasiswa: React.FC = () => {
  return (
    <AppLayout title="Identifikasi">
      <Identifikasi />
    </AppLayout>
  );
};

export default IdentifikasiMahasiswa;
